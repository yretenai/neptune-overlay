diff --git a/CMakeLists.txt b/CMakeLists.txt
index de97d49bf..829af51b9 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -382,16 +382,12 @@ endif()
 
 # Fetch Tracy
 if(BUILD_WITH_TRACY)
-	include(FetchContent)
-
-	message(STATUS "Fetching Tracy (${FETCH_TRACY_GIT_TAG})...")
-	FetchContent_Declare(
-		tracy
-		GIT_REPOSITORY https://github.com/wolfpld/tracy.git
-		GIT_TAG ${FETCH_TRACY_GIT_TAG}
-		GIT_SHALLOW TRUE
-		GIT_PROGRESS TRUE
-	)
-	FetchContent_MakeAvailable(tracy)
-	message(STATUS "Fetching Tracy - done")
+	find_package(PkgConfig QUIET)
+	pkg_search_module(TRACY REQUIRED tracy)
+	add_library(TracyClient INTERFACE)
+	target_include_directories(TracyClient INTERFACE ${TRACY_INCLUDEDIR})
+	target_compile_options(TracyClient INTERFACE ${TRACY_CFLAGS})
+	target_link_options(TracyClient INTERFACE ${TRACY_LDFLAGS})
+	target_link_libraries(TracyClient INTERFACE ${TRACY_LIBRARIES} ${TRACY_LINK_LIBRARIES})
+	add_library(Tracy::TracyClient ALIAS TracyClient)
 endif()
